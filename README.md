- **GrowGramMobile/** — Expo React Native App
- **GrowGramBackend/** — Firebase Functions Backend
- **scripts/** — Hilfsskripte

> Private Repo. Secrets liegen nur in lokalen `.env`-Dateien und sind nicht im Repo.
